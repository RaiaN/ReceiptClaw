[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "receipt-processor"
version = "0.1.0"
description = "Hackathon MVP for Tesco flatmate billing workflows"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["receipt_processor"]

